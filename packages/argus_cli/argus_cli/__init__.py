"""Argus CLI — interactive REPL and one-shot commands for Argus MCP.

This package provides:
- A dual-mode CLI (REPL when invoked with no args, one-shot when given a command)
- A TUI frontend (requires ``argus-cli[tui]`` extra)
- 16 themed color schemes
- Tab completion, history, session aliases, watch mode
"""

__version__ = "0.1.0"
