[package]
name = "ffi-guard-rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "ffi_guard_rs"
crate-type = ["rlib"]

[dependencies]
pyo3 = { version = "0.28", features = ["abi3-py310"] }
log = "0.4"
