"""Shared API layer for Argus MCP.

This package holds client-safe types and the management API HTTP client.
Both the server (producing responses) and any client (TUI, REPL, SDK)
import models from here so the API contract lives in one place.
"""
